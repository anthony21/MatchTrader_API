# Version workspace

The active application is `v4/`. Read `v4/AGENTS.md` and `v4/agent.md` before work.
Run application commands from `v4/` using its own `.venv`.

Preserve `v1/`, `v2/` and `v3/`. v4 adds native Quantower capture, persistent trade IDs,
explicit demo routing and the Orders workspace page. Read `v4/docs/QUANTOWER_CAPTURE.md`
for remaining source attribution and position-mapping limits. Copying is disarmed
until the source account and quantity conversion are configured.
Do not copy credentials or live journals into a distributable source snapshot.

Read `v4/docs/MANUAL_COPYING.md` for P01 attribution, UI settings and current live validation limits.
